title: Inahoya Notes
description: Physics Note
mathjax: true
remote_theme: "mmistakes/minimal-mistakes"

plugins:
  - jekyll-include-cache

# サイドバー設定
defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: single
      sidebar:
        nav: "main"
